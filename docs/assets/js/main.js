"use strict";const listCharacters=document.querySelector(".js-characters-list");let characters=[];const searchBtn=document.querySelector(".js-search-btn"),searchBar=document.querySelector(".js-search-bar");let favorites=[];function renderCharacters(e){let a="";for(const r of e)a+='<li class="item js-character"> <article class="character">',a+=`<img class="character__image"src="${r.img}">`,a+=`<h2 class="character__name">${r.name}</h2>`,a+=`<p class="character__dead">${r.status}</p>`,a+="</article> </li>";listCharacters.innerHTML=a,favoriteCharacters()}function getData(){fetch("https://breakingbadapi.com/api/characters").then(e=>e.json()).then(e=>{characters=e,renderCharacters(characters)})}function handleClickbtn(e){e.preventDefault();const a=searchBar.value.toLowerCase();renderCharacters(characters.filter(e=>e.name.toLowerCase().includes(a)))}function handleClickfav(){console.log(event.currentTarget)}function favoriteCharacters(){const e=document.querySelectorAll(".js-character");for(const a of e)a.addEventListener("click",handleClickfav)}getData(),searchBtn.addEventListener("click",handleClickbtn);