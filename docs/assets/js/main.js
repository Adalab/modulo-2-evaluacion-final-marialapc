"use strict";const listCharacters=document.querySelector(".js-characters-list");let characters=[];const searchBar=document.querySelector(".js-search-bar");function renderCharacters(e){let a="";for(const r of e)a+='<li class="item"><article class="character">',a+=`<img class="character__image"src="${r.img}">`,a+=`<h2 class="character__name">${r.name}</h2>`,a+=`<p class="character__dead">${r.status}</p>`,a+="</article> </li>";listCharacters.innerHTML=a}function getData(){fetch("https://breakingbadapi.com/api/characters").then(e=>e.json()).then(e=>{characters=e,renderCharacters(characters)})}function handleKeyup(){const e=searchBar.value.toLowerCase();renderCharacters(characters.filter(a=>a.name.toLowerCase().includes(e)))}getData(),searchBar.addEventListener("keyup",handleKeyup);